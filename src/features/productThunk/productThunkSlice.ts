import { createSlice, PayloadAction } from "@reduxjs/toolkit";


const productThunkSllice = createSlice({
    name: 'productThunk',
    initialState: '',
    reducers: {

    }
})

// actions


// reducer
const productThunkReducer = productThunkSllice.reducer;
export default productThunkReducer;